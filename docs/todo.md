# Arena Logger TODO

DevContainer は使わず、ローカル環境で開発する前提の TODO。

## 0. 現状整理

- [x] 要件と設計方針を `docs/design.md` に整理する
- [x] サンプル画像を `samples/` に配置する
- [x] サンプル画像基準の座標を `docs/coordinate-spec.md` に整理する
- [x] `docs/design.md` の「現時点で未定のもの」が、`docs/coordinate-spec.md` で解決済みか確認して更新する
- [x] 不要になった DevContainer 関連ファイルやメモが残っていないか確認する
- [x] 初回コミット対象にするファイルを決める

## 1. ローカル開発環境

- [x] Node.js の利用バージョンを決める
  - 候補: Node.js 24
- [x] パッケージマネージャーを決める
  - 候補: npm
- [x] Node.js と npm をローカル環境へインストールする
- [x] `package.json` を作成する
- [x] Vite + React + TypeScript のプロジェクトを作成する
- [x] `npm run dev` で開発サーバーが起動することを確認する
- [x] `npm run build` で静的ビルドが通ることを確認する
- [x] `README.md` にローカル起動手順を書く

## 2. 基本構成

- [x] ディレクトリ構成を決める
  - 候補: `src/components`, `src/lib`, `src/types`, `src/constants`
- [x] TypeScript の基本型を定義する
  - `BattleRecord`
  - `BattleSide`
  - `BattleCharacter`
  - `RelativeRect`
  - `ExtractionError`
- [x] 座標定義をコード化する
  - `docs/coordinate-spec.md` の relative 値を利用する
- [x] ファイル名から `battle_time` を抽出する関数を実装する
- [x] CSV ヘッダー定義をコード化する
- [x] CSV 生成関数を実装する

## 3. 画像入力 UI

- [x] 画像アップロード UI を作る
- [x] 複数画像を選択できるようにする
- [x] 対象ファイル名のバリデーションを行う
- [x] 選択済み画像の一覧を表示する
- [ ] 処理前、処理中、成功、失敗の状態を表示する
- [ ] エラー時に対象ファイル名と項目名が分かるようにする

## 4. 画像切り出し

- [x] 画像ファイルをブラウザで読み込む処理を実装する
- [x] `RelativeRect` から実ピクセル座標へ変換する関数を実装する
- [x] Canvas で指定領域を切り出す関数を実装する
- [x] 切り出した領域をデバッグ表示できる簡易 UI を用意する
- [ ] サンプル画像で各領域が意図通り切り出せるか確認する

## 5. OCR

- [ ] OCR ライブラリを選定する
  - 第一候補: `tesseract.js`
- [ ] OCR ライブラリを導入する
- [ ] OCR worker の初期化と破棄の流れを実装する
- [ ] Win/Lose 領域の OCR を実装する
- [ ] ユーザー名領域の OCR を実装する
- [ ] キャラ名領域の OCR を実装する
- [ ] ダメージ領域の OCR を実装する
- [ ] OCR 結果の正規化処理を実装する
  - `win` / `lose`
  - ユーザー名の trim
  - キャラ名の改行整理
  - ダメージの数値化
- [ ] OCR 精度が悪い領域の前処理を追加する
  - グレースケール
  - 二値化
  - 拡大
  - コントラスト調整

## 6. 攻撃/防衛アイコン判定

- [ ] 攻撃アイコンと防衛アイコンのテンプレート画像を用意する
- [ ] テンプレート画像の置き場所を決める
  - 候補: `public/templates/`
- [ ] ロールアイコン領域を切り出す
- [ ] テンプレート比較方法を決める
  - 初期案: Canvas のピクセル差分で比較する
- [ ] 類似度スコアを計算する関数を実装する
- [ ] 閾値を決める
- [ ] 判定失敗時のエラーを実装する
- [ ] 左右どちらが攻撃/防衛でも CSV では役割ベースに並び替える

## 7. 抽出パイプライン

- [ ] 1 画像を `BattleRecord` に変換する関数を実装する
- [ ] 複数画像を順番に処理する
- [ ] ブラウザ負荷を見ながら並列数を決める
- [ ] 途中失敗時は処理を中断する
- [ ] 成功した `BattleRecord` を `battle_time` 順に並べる
- [ ] CSV 文字列を生成して画面に表示する

## 8. CSV 表示 UI

- [x] CSV 表示用のテキストエリアを作る
- [x] コピー用ボタンを作る
- [x] CSV が空のときの表示を決める
- [ ] 処理完了件数を表示する
- [ ] 失敗時は CSV を更新しない

## 9. テスト

- [x] `battle_time` 抽出の単体テストを書く
- [x] `RelativeRect` 変換の単体テストを書く
- [x] CSV 生成の単体テストを書く
- [ ] OCR 正規化処理の単体テストを書く
- [ ] 攻撃/防衛の並び替え処理の単体テストを書く
- [ ] サンプル画像を使った手動検証手順を書く

## 10. 最初の実装順

1. Vite + React + TypeScript を作成する
2. 型定義、座標定義、`battle_time` 抽出、CSV 生成を実装する
3. 画像アップロード UI を作る
4. Canvas で座標切り出しを実装する
5. OCR を 1 項目だけつないで精度を見る
6. 攻撃/防衛アイコン判定を実装する
7. 全項目の抽出パイプラインに広げる
8. CSV 表示とコピー UI を仕上げる
9. テストと README を整える

## 11. 判断待ち

- [x] Node.js のバージョンを固定するか
- [x] npm 以外のパッケージマネージャーを使うか
- [x] OCR は `tesseract.js` で始めるか
- [x] テンプレート比較を自前実装にするか、画像処理ライブラリを使うか
- [ ] ダメージ領域を現状の広い座標のまま進めるか、数値付近へ狭めるか
